<Window x:Class="MovieIntro.BotHelpDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Облачные АТС"
        WindowStartupLocation="CenterOwner"
        Width="650" Height="500"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="0"
                      ResizeBorderThickness="0"
                      GlassFrameThickness="0"
                      CornerRadius="0"/>
    </WindowChrome.WindowChrome>

    <!-- Основной контейнер с черным фоном и светлой окантовкой -->
    <Border Background="#0A0A0A" 
            BorderBrush="#333333" 
            CornerRadius="8"
            BorderThickness="1">

        <Grid Margin="4">
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Заголовок с кнопкой закрытия -->
            <Border Grid.Row="0" 
                    Background="#0A0A0A" 
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid>
                    <TextBlock Text="ℹ️ ЧАТ БОТЫ"
                               FontFamily="Segoe UI"
                               FontSize="14"
                               FontWeight="SemiBold"
                               Foreground="#FFFFFF"
                               VerticalAlignment="Center"
                               Margin="15,0,0,0"/>

                    <!-- Кнопка закрытия -->
                    <Button x:Name="CloseButton"
                            Width="30" Height="30"
                            HorizontalAlignment="Right"
                            Margin="0,0,10,0"
                            Cursor="Hand"
                            Background="Transparent"
                            BorderThickness="0"
                            Click="CloseButton_Click">
                        <Button.Content>
                            <Grid Width="12" Height="12">
                                <Line X1="0" Y1="0" X2="12" Y2="12" 
                                      Stroke="#666666" StrokeThickness="2"/>
                                <Line X1="12" Y1="0" X2="0" Y2="12" 
                                      Stroke="#666666" StrokeThickness="2"/>
                            </Grid>
                        </Button.Content>
                        <Button.Style>
                            <Style TargetType="Button">
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Content">
                                            <Setter.Value>
                                                <Grid Width="12" Height="12">
                                                    <Line X1="0" Y1="0" X2="12" Y2="12" 
                                                          Stroke="#FF3333" StrokeThickness="2"/>
                                                    <Line X1="12" Y1="0" X2="0" Y2="12" 
                                                          Stroke="#FF3333" StrokeThickness="2"/>
                                                </Grid>
                                            </Setter.Value>
                                        </Setter>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
            </Border>

            <!-- Основное содержимое -->
            <ScrollViewer Grid.Row="1" 
                          VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled">
                <StackPanel Margin="20,15,20,20">

                    <!-- Пояснительный текст -->
                    <TextBlock Text="Нам потребуется чат бот для вывода сообщения:"
                               FontFamily="Segoe UI"
                               FontSize="13"
                               Foreground="#DDDDDD"
                               TextWrapping="Wrap"
                               Margin="0,0,0,20"
                               LineHeight="20"/>

                    <!-- Список преимуществ -->
                    <StackPanel Margin="10,0,0,20">
                        <TextBlock Text="✓ Получаем сообщение в телефоне по кнопке"
                                   FontFamily="Segoe UI"
                                   FontSize="13"
                                   Foreground="#CCCCCC"
                                   Margin="0,3,0,3"/>
                        <TextBlock Text="✓ Получаем сообщение в телефоне по рассписанию"
                                   FontFamily="Segoe UI"
                                   FontSize="13"
                                   Foreground="#CCCCCC"
                                   Margin="0,3,0,3"/>
                        <TextBlock Text="✓ Добавляем чат бота в общую группу для уведомлений"
                                   FontFamily="Segoe UI"
                                   FontSize="13"
                                   Foreground="#CCCCCC"
                                   Margin="0,3,0,3"/>
                        <TextBlock Text="✓ Движемся к подключению 1С"
                                   FontFamily="Segoe UI"
                                   FontSize="13"
                                   Foreground="#CCCCCC"
                                   Margin="0,3,0,3"/>
                    </StackPanel>

                    <!-- Разделитель -->
                    <Rectangle Height="1" Fill="#333333" Margin="0,0,0,20"/>

                    <TextBlock Text="ВЫБЕРИТЕ ПРОВАЙДЕРА"
                               FontFamily="Segoe UI"
                               FontSize="16"
                               FontWeight="SemiBold"
                               Foreground="#FFFFFF"
                               HorizontalAlignment="Center"
                               Margin="0,0,0,20"/>

                    <!-- Карточки провайдеров -->
                    <Grid Margin="0,10,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Билайн -->
                        <Border Grid.Column="0"
                                Background="#0D0D0D"
                                BorderBrush="#333333"
                                BorderThickness="1"
                                CornerRadius="8"
                                Cursor="Hand"
                                MouseLeftButtonDown="Telegram_MouseLeftButtonDown">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#1A1A1A"/>
                                            <Setter Property="BorderBrush" Value="#666666"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>

                            <StackPanel Margin="20,15,20,15">
                                <Image Source="/Assets/telegram_icon_b.png" Width="60" Height="60" Margin="0,0,0,10"/>
                                <TextBlock Text="Телеграмм бот"
                                           FontFamily="Segoe UI"
                                           FontSize="16"
                                           FontWeight="Bold"
                                           Foreground="#FFFFFF"
                                           HorizontalAlignment="Center"
                                           Margin="0,0,0,5"/>
                                <TextBlock Text="Чат бот"
                                           FontFamily="Segoe UI"
                                           FontSize="12"
                                           Foreground="#888888"
                                           HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Border>

                        <!-- Tele2 -->
                        <Border Grid.Column="2"
                                Background="#0D0D0D"
                                BorderBrush="#333333"
                                BorderThickness="1"
                                CornerRadius="8"
                                Cursor="Hand"
                                MouseLeftButtonDown="Max_MouseLeftButtonDown">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#1A1A1A"/>
                                            <Setter Property="BorderBrush" Value="#666666"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>

                            <StackPanel Margin="20,15,20,15">
                                <Image Source="/Assets/max_icon_b.png" Width="60" Height="60" Margin="0,0,0,10"/>
                                <TextBlock Text="Макс бот"
                                           FontFamily="Segoe UI"
                                           FontSize="16"
                                           FontWeight="Bold"
                                           Foreground="#FFFFFF"
                                           HorizontalAlignment="Center"
                                           Margin="0,0,0,5"/>
                                <TextBlock Text="Чат бот"
                                           FontFamily="Segoe UI"
                                           FontSize="12"
                                           Foreground="#888888"
                                           HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                    </Grid>

                    <TextBlock Text="Следуйте инструкции по созданию бота. Полученный токен скопируйте в поле ввода"
                               FontFamily="Segoe UI"
                               FontSize="11"
                               Foreground="#999999"
                               HorizontalAlignment="Center"
                               Margin="0,10,0,0"/>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Border>
</Window>